---
"@supabase/pg-topo": patch
---

Preserve local built-in-looking range, operator, and operator-class support objects, recognize built-in range subtype-diff helpers, normalize PostgreSQL array, row-type array, and multirange array type references, and report missing default range subtype opclasses.
