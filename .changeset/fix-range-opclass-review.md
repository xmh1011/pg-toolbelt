---
"@supabase/pg-topo": patch
---

Fix range subtype default opclass diagnostics for built-in subtypes, schema-qualified array types that shadow built-ins, and external default opclass provider subtype matching.
